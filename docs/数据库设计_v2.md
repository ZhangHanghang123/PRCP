# PRCP 数据库设计 v2.0 — 经营计划与资产负债预测平台

> 配套产品原型：`docs/产品原型设计_v2.md`
> 配套技术架构：`docs/技术架构_v2.md`

## 1. 设计总览

### 1.1 表清单（25 张业务表 + 4 张系统表 = 29 张）

```
┌─ 公共字段 ─────────────────────────────────────────┐
│ id BIGINT AUTO_INCREMENT PRIMARY KEY              │
│ is_deleted TINYINT(1) DEFAULT 0                   │
│ created_by BIGINT                                 │
│ updated_by BIGINT                                 │
│ created_at DATETIME DEFAULT CURRENT_TIMESTAMP     │
│ updated_at DATETIME DEFAULT CURRENT_TIMESTAMP     │
│              ON UPDATE CURRENT_TIMESTAMP          │
└────────────────────────────────────────────────────┘

[账户册模块 4 张]
  sys_user, sys_role, sys_permission, sys_role_permission,
  prcp_coa_scheme,           -- 账户册方案
  prcp_coa_node,             -- 账户册节点（树形）
  prcp_dict_type,            -- 字典分类
  prcp_dict_item,            -- 字典项

[报表模块 2 张]
  prcp_rpt_report,           -- 报表定义（6 类）
  prcp_rpt_item,             -- 报表表项（树形）

[数据维护模块 7 张]
  prcp_data_balance,         -- 资产负债表（24 月缺口）
  prcp_data_finance,         -- 账务结果指标
  prcp_data_key_param,       -- 关键参数指标
  prcp_data_scale,           -- 规模指标
  prcp_data_price,           -- 价格指标
  prcp_data_fee,             -- 中收指标
  prcp_data_rwa,             -- 资本与 RWA 假设指标

[指标管理模块 2 张]
  prcp_kpi_definition,       -- 指标定义（含公式）
  prcp_kpi_value,            -- 指标值（按日期+版本）

[模型管理模块 3 张]
  prcp_model_version,        -- 模型参数版本（含父版本）
  prcp_model_param,          -- 模型参数（关联指标）
  prcp_model_train_task,     -- 训练任务

[组合反算模块 2 张]
  prcp_reverse_scenario,     -- 反算方案
  prcp_reverse_result,       -- 反算结果（20 年）

[审计日志 1 张]
  prcp_audit_log             -- 审计日志（所有写操作）
```

### 1.2 ER 关系图

```
sys_user ──→ sys_role ──→ sys_role_permission ──→ sys_permission
   │
   └──→ 所有业务表的 created_by / updated_by

prcp_coa_scheme (1) ──→ (N) prcp_coa_node
                              │ │
                              │ └──→ 自身（parent_id 树形）
                              │
                              └──→ prcp_data_balance / node_id

prcp_rpt_report (1) ──→ (N) prcp_rpt_item
                              │
                              ├──→ 自身（parent_id 树形）
                              └──→ prcp_kpi_definition / rpt_id

prcp_data_* (7 张指标) ──→ prcp_rpt_item / item_id
                       └──→ prcp_coa_node / node_id (分子分母)

prcp_kpi_definition (1) ──→ (N) prcp_kpi_value
                                    │
                                    └──→ 数据日期 + 版本

prcp_model_version (1) ──→ (N) prcp_model_param
                                    │
                                    └──→ prcp_kpi_definition / kpi_id

prcp_model_version (1) ──→ (N) prcp_model_train_task
                                    │
                                    └──→ prcp_data_balance / balance_id

prcp_reverse_scenario (1) ──→ (1) prcp_reverse_result
                                    │
                                    ├──→ prcp_model_version / version_id
                                    └──→ JSON 加载指标项
```

---

## 2. 系统表（4 张）

### 2.1 `sys_user` — 用户

```sql
CREATE TABLE sys_user (
  id              BIGINT AUTO_INCREMENT PRIMARY KEY,
  username        VARCHAR(64)  NOT NULL UNIQUE,
  password_hash   VARCHAR(128) NOT NULL,
  display_name    VARCHAR(64),
  email           VARCHAR(128),
  role_id         BIGINT,
  status          TINYINT(1) DEFAULT 1,        -- 1=ACTIVE 0=DISABLED
  is_deleted      TINYINT(1) DEFAULT 0,
  created_by      BIGINT,
  updated_by      BIGINT,
  created_at      DATETIME DEFAULT CURRENT_TIMESTAMP,
  updated_at      DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  INDEX idx_username (username),
  INDEX idx_role (role_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

### 2.2 `sys_role` — 角色

```sql
CREATE TABLE sys_role (
  id              BIGINT AUTO_INCREMENT PRIMARY KEY,
  role_code       VARCHAR(32) NOT NULL UNIQUE,    -- admin/analyst/viewer
  role_name       VARCHAR(64) NOT NULL,
  description     TEXT,
  status          TINYINT(1) DEFAULT 1,
  is_deleted      TINYINT(1) DEFAULT 0,
  created_by      BIGINT,
  updated_by      BIGINT,
  created_at      DATETIME DEFAULT CURRENT_TIMESTAMP,
  updated_at      DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

### 2.3 `sys_permission` — 权限

```sql
CREATE TABLE sys_permission (
  id              BIGINT AUTO_INCREMENT PRIMARY KEY,
  perm_key        VARCHAR(64) NOT NULL UNIQUE,    -- 'coa:read' 'model:train' ...
  perm_name       VARCHAR(64) NOT NULL,
  menu_path        VARCHAR(128),                  -- 关联前端路由
  description     TEXT,
  created_at      DATETIME DEFAULT CURRENT_TIMESTAMP
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

### 2.4 `sys_role_permission` — 角色权限关联

```sql
CREATE TABLE sys_role_permission (
  id              BIGINT AUTO_INCREMENT PRIMARY KEY,
  role_id         BIGINT NOT NULL,
  perm_id         BIGINT NOT NULL,
  UNIQUE KEY uk_role_perm (role_id, perm_id),
  INDEX idx_role (role_id),
  INDEX idx_perm (perm_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

---

## 3. 账户册模块（4 张）

### 3.1 `prcp_coa_scheme` — 账户册方案

```sql
CREATE TABLE prcp_coa_scheme (
  id              BIGINT AUTO_INCREMENT PRIMARY KEY,
  scheme_code     VARCHAR(32) NOT NULL UNIQUE,     -- BS_2026
  scheme_name     VARCHAR(64) NOT NULL,           -- 资负表方案
  description     TEXT,
  status          VARCHAR(16) DEFAULT 'ACTIVE',   -- ACTIVE/INACTIVE
  node_count      INT DEFAULT 0,                  -- 冗余，加速列表
  is_deleted      TINYINT(1) DEFAULT 0,
  created_by      BIGINT,
  updated_by      BIGINT,
  created_at      DATETIME DEFAULT CURRENT_TIMESTAMP,
  updated_at      DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  INDEX idx_code (scheme_code),
  INDEX idx_status (status)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

### 3.2 `prcp_coa_node` — 账户册节点（树形）

```sql
CREATE TABLE prcp_coa_node (
  id              BIGINT AUTO_INCREMENT PRIMARY KEY,
  scheme_id       BIGINT NOT NULL,                  -- FK → prcp_coa_scheme
  node_code       VARCHAR(32) NOT NULL,             -- 方案内唯一
  node_name       VARCHAR(64) NOT NULL,
  parent_id       BIGINT,                            -- 自引用 NULL=根
  node_level      INT DEFAULT 1,                     -- 冗余，触发器更新
  node_type       VARCHAR(16),                       -- ASSET/LIABILITY/EQUITY/INCOME/EXPENSE
  path            VARCHAR(255),                      -- 物化路径: '/1/2/3/'
  sort_order      INT DEFAULT 0,
  status          VARCHAR(16) DEFAULT 'ACTIVE',
  description     TEXT,
  is_deleted      TINYINT(1) DEFAULT 0,
  created_by      BIGINT,
  updated_by      BIGINT,
  created_at      DATETIME DEFAULT CURRENT_TIMESTAMP,
  updated_at      DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  UNIQUE KEY uk_scheme_code (scheme_id, node_code),
  INDEX idx_parent (parent_id),
  INDEX idx_scheme (scheme_id),
  INDEX idx_path (path)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

**树形查询**：
```sql
-- 整棵树（按 path 排序天然有序）
SELECT * FROM prcp_coa_node WHERE scheme_id=? AND is_deleted=0 ORDER BY path, sort_order;

-- 查所有子节点
SELECT * FROM prcp_coa_node WHERE path LIKE '/1/2/%' AND is_deleted=0;
```

### 3.3 `prcp_dict_type` — 字典分类

```sql
CREATE TABLE prcp_dict_type (
  id              BIGINT AUTO_INCREMENT PRIMARY KEY,
  dict_type       VARCHAR(32) NOT NULL UNIQUE,    -- ASSET_TYPE/CURRENCY/TENOR
  dict_name       VARCHAR(64) NOT NULL,
  description     TEXT,
  is_deleted      TINYINT(1) DEFAULT 0,
  created_by      BIGINT,
  updated_by      BIGINT,
  created_at      DATETIME DEFAULT CURRENT_TIMESTAMP,
  updated_at      DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

### 3.4 `prcp_dict_item` — 字典项

```sql
CREATE TABLE prcp_dict_item (
  id              BIGINT AUTO_INCREMENT PRIMARY KEY,
  dict_type_id    BIGINT NOT NULL,
  item_key        VARCHAR(64) NOT NULL,
  item_value      VARCHAR(128) NOT NULL,
  sort_order      INT DEFAULT 0,
  status          TINYINT(1) DEFAULT 1,
  is_deleted      TINYINT(1) DEFAULT 0,
  created_by      BIGINT,
  updated_by      BIGINT,
  created_at      DATETIME DEFAULT CURRENT_TIMESTAMP,
  updated_at      DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  UNIQUE KEY uk_type_key (dict_type_id, item_key),
  INDEX idx_type (dict_type_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

---

## 4. 报表模块（2 张）

### 4.1 `prcp_rpt_report` — 报表定义（6 类）

```sql
CREATE TABLE prcp_rpt_report (
  id              BIGINT AUTO_INCREMENT PRIMARY KEY,
  report_code     VARCHAR(32) NOT NULL UNIQUE,    -- RPT_BS / RPT_PL ...
  report_name     VARCHAR(64) NOT NULL,
  report_type     VARCHAR(16) NOT NULL,           -- BALANCE/INCOME/CASHFLOW/INDICATOR/RISK/LIQUIDITY
  scheme_id       BIGINT NOT NULL,                -- 关联账户册方案
  description     TEXT,
  item_count      INT DEFAULT 0,                   -- 冗余
  status          VARCHAR(16) DEFAULT 'ACTIVE',
  is_deleted      TINYINT(1) DEFAULT 0,
  created_by      BIGINT,
  updated_by      BIGINT,
  created_at      DATETIME DEFAULT CURRENT_TIMESTAMP,
  updated_at      DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  INDEX idx_type (report_type),
  INDEX idx_scheme (scheme_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

### 4.2 `prcp_rpt_item` — 报表表项（树形）

```sql
CREATE TABLE prcp_rpt_item (
  id              BIGINT AUTO_INCREMENT PRIMARY KEY,
  report_id       BIGINT NOT NULL,                  -- FK → prcp_rpt_report
  item_code       VARCHAR(32) NOT NULL,             -- 报表内唯一
  item_name       VARCHAR(64) NOT NULL,
  parent_id       BIGINT,                            -- 自引用
  item_level      INT DEFAULT 1,
  data_type       VARCHAR(16) DEFAULT 'DECIMAL',    -- DECIMAL/INT/TEXT
  formula         TEXT,                              -- 计算公式
  coa_node_ids    JSON,                              -- 关联账户册节点（分子分母多选）
  path            VARCHAR(255),
  sort_order      INT DEFAULT 0,
  status          VARCHAR(16) DEFAULT 'ACTIVE',
  description     TEXT,
  is_deleted      TINYINT(1) DEFAULT 0,
  created_by      BIGINT,
  updated_by      BIGINT,
  created_at      DATETIME DEFAULT CURRENT_TIMESTAMP,
  updated_at      DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  UNIQUE KEY uk_report_code (report_id, item_code),
  INDEX idx_parent (parent_id),
  INDEX idx_report (report_id),
  INDEX idx_path (path)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

---

## 5. 数据维护模块（7 张）

### 5.1 `prcp_data_balance` — 资产负债表（含 24 月缺口）

```sql
CREATE TABLE prcp_data_balance (
  id              BIGINT AUTO_INCREMENT PRIMARY KEY,
  coa_node_id     BIGINT NOT NULL,                  -- FK → 账户册节点
  data_date       DATE NOT NULL,
  current_amount    DECIMAL(20,4) DEFAULT 0,
  -- 未来 24 个月现金流缺口（M1 - M24）
  m1_gap          DECIMAL(20,4) DEFAULT 0,
  m2_gap          DECIMAL(20,4) DEFAULT 0,
  m3_gap          DECIMAL(20,4) DEFAULT 0,
  m4_gap          DECIMAL(20,4) DEFAULT 0,
  m5_gap          DECIMAL(20,4) DEFAULT 0,
  m6_gap          DECIMAL(20,4) DEFAULT 0,
  m7_gap          DECIMAL(20,4) DEFAULT 0,
  m8_gap          DECIMAL(20,4) DEFAULT 0,
  m9_gap          DECIMAL(20,4) DEFAULT 0,
  m10_gap         DECIMAL(20,4) DEFAULT 0,
  m11_gap         DECIMAL(20,4) DEFAULT 0,
  m12_gap         DECIMAL(20,4) DEFAULT 0,
  m13_gap         DECIMAL(20,4) DEFAULT 0,
  m14_gap         DECIMAL(20,4) DEFAULT 0,
  m15_gap         DECIMAL(20,4) DEFAULT 0,
  m16_gap         DECIMAL(20,4) DEFAULT 0,
  m17_gap         DECIMAL(20,4) DEFAULT 0,
  m18_gap         DECIMAL(20,4) DEFAULT 0,
  m19_gap         DECIMAL(20,4) DEFAULT 0,
  m20_gap         DECIMAL(20,4) DEFAULT 0,
  m21_gap         DECIMAL(20,4) DEFAULT 0,
  m22_gap         DECIMAL(20,4) DEFAULT 0,
  m23_gap         DECIMAL(20,4) DEFAULT 0,
  m24_gap         DECIMAL(20,4) DEFAULT 0,
  calc_note       TEXT,                              -- 缺口计算说明
  is_deleted      TINYINT(1) DEFAULT 0,
  created_by      BIGINT,
  updated_by      BIGINT,
  created_at      DATETIME DEFAULT CURRENT_TIMESTAMP,
  updated_at      DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  UNIQUE KEY uk_node_date (coa_node_id, data_date),
  INDEX idx_date (data_date),
  INDEX idx_node (coa_node_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

### 5.2 6 类指标数据表（统一结构）

6 张表结构完全一致，仅表名不同。字段说明：

```sql
-- 通用结构（以 prcp_data_finance 为例，其他 5 张类同）
CREATE TABLE prcp_data_finance (     -- 账务结果指标
  id              BIGINT AUTO_INCREMENT PRIMARY KEY,
  rpt_item_id     BIGINT NOT NULL,                  -- FK → 报表表项
  coa_node_id     BIGINT,                            -- 关联账户册节点（分子或分母项的主节点）
  data_date       DATE NOT NULL,
  current_value   DECIMAL(20,6),
  numerator_coa_nodes JSON,                            -- 分子项：账户册节点 ID 列表
  denominator_coa_nodes JSON,                          -- 分母项：账户册节点 ID 列表
  calc_formula    TEXT,                              -- 计算公式快照
  description     TEXT,
  is_deleted      TINYINT(1) DEFAULT 0,
  created_by      BIGINT,
  updated_by      BIGINT,
  created_at      DATETIME DEFAULT CURRENT_TIMESTAMP,
  updated_at      DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  UNIQUE KEY uk_item_date (rpt_item_id, data_date),
  INDEX idx_date (data_date),
  INDEX idx_item (rpt_item_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

-- 6 张表的命名
prcp_data_finance          -- 账务结果指标
prcp_data_key_param        -- 关键参数指标
prcp_data_scale            -- 规模指标
prcp_data_price            -- 价格指标
prcp_data_fee              -- 中收指标
prcp_data_rwa              -- 资本与 RWA 假设指标
```

**说明**：xlsx 要求 6 类指标都"按数据日期和报表结构维护"，且分子分母项选择账户册节点，因此 6 张表结构一致。前端用同一个页面组件，根据 `data_type` 切换表。

---

## 6. 指标管理模块（2 张）

### 6.1 `prcp_kpi_definition` — 指标定义

```sql
CREATE TABLE prcp_kpi_definition (
  id              BIGINT AUTO_INCREMENT PRIMARY KEY,
  kpi_code        VARCHAR(32) NOT NULL UNIQUE,      -- KPI_NIM / KPI_CAR
  kpi_name        VARCHAR(64) NOT NULL,
  rpt_id          BIGINT NOT NULL,                   -- 所属报表
  formula         TEXT NOT NULL,                     -- 引用同报表表项
  calc_unit       VARCHAR(16) DEFAULT 'PERCENT',     -- PERCENT/BP/RATIO/AMOUNT
  formula_desc    TEXT,
  threshold_min   DECIMAL(20,6),                    -- 阈值下限（监管要求）
  threshold_max   DECIMAL(20,6),                    -- 阈值上限
  status          VARCHAR(16) DEFAULT 'ACTIVE',
  is_deleted      TINYINT(1) DEFAULT 0,
  created_by      BIGINT,
  updated_by      BIGINT,
  created_at      DATETIME DEFAULT CURRENT_TIMESTAMP,
  updated_at      DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  INDEX idx_rpt (rpt_id),
  INDEX idx_code (kpi_code)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

### 6.2 `prcp_kpi_value` — 指标值

```sql
CREATE TABLE prcp_kpi_value (
  id              BIGINT AUTO_INCREMENT PRIMARY KEY,
  kpi_id          BIGINT NOT NULL,                    -- FK → prcp_kpi_definition
  data_date       DATE NOT NULL,
  version         VARCHAR(32) NOT NULL DEFAULT 'V1.0',
  current_value   DECIMAL(20,6),
  prev_value      DECIMAL(20,6),                       -- 上一期值（环比）
  prev_year_value DECIMAL(20,6),                       -- 去年同期（同比）
  calc_source     VARCHAR(16) DEFAULT 'MANUAL',        -- MANUAL / MODEL / RECALC
  calc_log        LONGTEXT,                             -- 计算日志
  is_deleted      TINYINT(1) DEFAULT 0,
  created_by      BIGINT,
  updated_by      BIGINT,
  created_at      DATETIME DEFAULT CURRENT_TIMESTAMP,
  updated_at      DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  UNIQUE KEY uk_kpi_date_ver (kpi_id, data_date, version),
  INDEX idx_date (data_date),
  INDEX idx_version (version)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

---

## 7. 模型管理模块（3 张）

### 7.1 `prcp_model_version` — 模型参数版本

```sql
CREATE TABLE prcp_model_version (
  id              BIGINT AUTO_INCREMENT PRIMARY KEY,
  version_code    VARCHAR(32) NOT NULL UNIQUE,         -- V1.0 / V2.0
  version_name    VARCHAR(64) NOT NULL,
  parent_version  VARCHAR(32),                          -- 父版本编码（自引用）
  param_count     INT DEFAULT 0,                        -- 冗余
  description     TEXT,
  status          VARCHAR(16) DEFAULT 'DRAFT',         -- DRAFT/ACTIVE/DEPRECATED
  is_deleted      TINYINT(1) DEFAULT 0,
  created_by      BIGINT,
  updated_by      BIGINT,
  created_at      DATETIME DEFAULT CURRENT_TIMESTAMP,
  updated_at      DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  INDEX idx_code (version_code),
  INDEX idx_parent (parent_version)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

### 7.2 `prcp_model_param` — 模型参数

```sql
CREATE TABLE prcp_model_param (
  id              BIGINT AUTO_INCREMENT PRIMARY KEY,
  version_id      BIGINT NOT NULL,                      -- FK → prcp_model_version
  param_code      VARCHAR(32) NOT NULL,                  -- 版本内唯一
  param_name      VARCHAR(64) NOT NULL,
  kpi_id          BIGINT,                                -- FK → prcp_kpi_definition（xlsx 要求）
  param_type       VARCHAR(16) DEFAULT 'RATIO',           -- RATIO / AMOUNT / FLAG
  default_value   DECIMAL(20,6),
  current_value   DECIMAL(20,6),
  description     TEXT,
  sort_order      INT DEFAULT 0,
  status          VARCHAR(16) DEFAULT 'ACTIVE',
  is_deleted      TINYINT(1) DEFAULT 0,
  created_by      BIGINT,
  updated_by      BIGINT,
  created_at      DATETIME DEFAULT CURRENT_TIMESTAMP,
  updated_at      DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  UNIQUE KEY uk_version_code (version_id, param_code),
  INDEX idx_version (version_id),
  INDEX idx_kpi (kpi_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

### 7.3 `prcp_model_train_task` — 训练任务

```sql
CREATE TABLE prcp_model_train_task (
  id              BIGINT AUTO_INCREMENT PRIMARY KEY,
  task_code       VARCHAR(32) NOT NULL UNIQUE,           -- TRN_001
  task_name       VARCHAR(64) NOT NULL,
  version_id      BIGINT NOT NULL,                       -- FK → 模型参数版本
  balance_id      BIGINT NOT NULL,                       -- FK → 资产负债表快照 ID
  status          VARCHAR(16) DEFAULT 'PENDING',         -- PENDING/RUNNING/COMPLETE/FAILED
  progress        INT DEFAULT 0,                         -- 0-100
  started_at      DATETIME,
  finished_at     DATETIME,
  duration_sec    INT,
  model_meta      LONGTEXT,                              -- JSON，模型元数据
  model_file_path VARCHAR(255),                          -- /data/models/{id}/model.pkl
  loss_curve      LONGTEXT,                              -- JSON，每 epoch 的 loss
  error_message   TEXT,
  is_deleted      TINYINT(1) DEFAULT 0,
  created_by      BIGINT,
  updated_by      BIGINT,
  created_at      DATETIME DEFAULT CURRENT_TIMESTAMP,
  updated_at      DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  INDEX idx_status (status),
  INDEX idx_version (version_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

---

## 8. 组合反算模块（2 张）

### 8.1 `prcp_reverse_scenario` — 反算方案

```sql
CREATE TABLE prcp_reverse_scenario (
  id              BIGINT AUTO_INCREMENT PRIMARY KEY,
  scenario_code   VARCHAR(32) NOT NULL UNIQUE,          -- RC_001
  scenario_name   VARCHAR(64) NOT NULL,
  version_id      BIGINT NOT NULL,                        -- FK → 模型参数版本
  loaded_kpis     JSON,                              -- [{kpi_id, target_value, weight}]
  description     TEXT,
  status          VARCHAR(16) DEFAULT 'PENDING',         -- PENDING/RUNNING/DONE/FAILED
  progress        INT DEFAULT 0,
  started_at      DATETIME,
  finished_at     DATETIME,
  duration_sec    INT,
  is_deleted      TINYINT(1) DEFAULT 0,
  created_by      BIGINT,
  updated_by      BIGINT,
  created_at      DATETIME DEFAULT CURRENT_TIMESTAMP,
  updated_at      DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  INDEX idx_status (status)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

### 8.2 `prcp_reverse_result` — 反算结果（20 年）

```sql
CREATE TABLE prcp_reverse_result (
  id              BIGINT AUTO_INCREMENT PRIMARY KEY,
  scenario_id     BIGINT NOT NULL,                         -- FK → prcp_reverse_scenario
  -- 未来 20 年报表（资负表 + 关键指标）
  result_json     LONGTEXT NOT NULL,
  -- 结构：
  -- {
  --   "balance": {"y1": {"BS_ASSET_TOTAL": ..., ...}, "y2": ..., ...},
  --   "kpi": {"y1": {"KPI_CAR": ..., ...}, ...}
  -- }
  year_count      INT DEFAULT 20,
  summary_json    LONGTEXT,                                -- 摘要
  is_deleted      TINYINT(1) DEFAULT 0,
  created_at      DATETIME DEFAULT CURRENT_TIMESTAMP,
  INDEX idx_scenario (scenario_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

---

## 9. 审计日志（1 张）

### 9.1 `prcp_audit_log` — 审计日志

```sql
CREATE TABLE prcp_audit_log (
  id              BIGINT AUTO_INCREMENT PRIMARY KEY,
  user_id         BIGINT,
  username        VARCHAR(64),
  operation       VARCHAR(32) NOT NULL,                    -- CREATE/UPDATE/DELETE/IMPORT/EXPORT/TRAIN/REVERSE
  module          VARCHAR(32) NOT NULL,                    -- COA/RPT/DATA/KPI/MODEL/REVERSE
  object_id       BIGINT,
  object_code     VARCHAR(64),
  before_value    LONGTEXT,                                  -- JSON
  after_value     LONGTEXT,                                  -- JSON
  ip_address      VARCHAR(64),
  user_agent      VARCHAR(255),
  created_at      DATETIME DEFAULT CURRENT_TIMESTAMP,
  INDEX idx_user (user_id),
  INDEX idx_module (module),
  INDEX idx_object (object_id),
  INDEX idx_time (created_at)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

---

## 10. 关键 SQL 视图（用于 Dashboard 和导出）

### 10.1 `v_prcp_kpi_latest` — 指标最新值

```sql
CREATE OR REPLACE VIEW v_prcp_kpi_latest AS
SELECT
  kd.id, kd.kpi_code, kd.kpi_name, kd.rpt_id,
  kv.data_date, kv.version, kv.current_value,
  kv.prev_value, kv.prev_year_value,
  (kv.current_value - kv.prev_value) / NULLIF(kv.prev_value, 0) AS mom_ratio,
  (kv.current_value - kv.prev_year_value) / NULLIF(kv.prev_year_value, 0) AS yoy_ratio,
  kd.threshold_min, kd.threshold_max,
  CASE WHEN kd.threshold_min IS NOT NULL AND kv.current_value < kd.threshold_min THEN 'BELOW_MIN'
       WHEN kd.threshold_max IS NOT NULL AND kv.current_value > kd.threshold_max THEN 'ABOVE_MAX'
       ELSE 'NORMAL' END AS threshold_status
FROM prcp_kpi_definition kd
LEFT JOIN prcp_kpi_value kv ON kv.kpi_id = kd.id
  AND kv.is_deleted = 0
  AND kv.data_date = (SELECT MAX(data_date) FROM prcp_kpi_value WHERE kpi_id = kd.id AND is_deleted = 0);
```

### 10.2 `v_prcp_balance_summary` — 资负表汇总

```sql
CREATE OR REPLACE VIEW v_prcp_balance_summary AS
SELECT
  cn.scheme_id, cs.scheme_code, cs.scheme_name,
  cn.node_type, cn.path,,
  SUM(db.current_amount) AS total_amount,
  SUM(COALESCE(db.m1_gap, 0)) AS m1_total,
  SUM(COALESCE(db.m12_gap, 0)) AS m12_total,
  SUM(COALESCE(db.m24_gap, 0)) AS m24_total,
  MAX(db.data_date) AS latest_date
FROM prcp_coa_node cn
JOIN prcp_coa_scheme cs ON cs.id = cn.scheme_id
LEFT JOIN prcp_data_balance db ON db.coa_node_id = cn.id AND db.is_deleted = 0
WHERE cn.is_deleted = 0 AND cn.parent_id IS NULL  -- 仅一级节点
GROUP BY cn.scheme_id, cs.scheme_code, cs.scheme_name, cn.node_type, cn.path;
```

---

## 11. 演示数据种子

### 11.1 初始数据（init_db.py 写入）

```python
DEMO_DATA = {
    "sys_role": [
        ("admin", "系统管理员", "所有权限"),
        ("analyst", "分析师", "建模/数据/指标读写"),
        ("viewer", "查看者", "只读"),
    ],
    "prcp_coa_scheme": [
        ("BS_2026", "资负表方案", "演示资负账户方案"),
        ("PL_2026", "损益表方案", "演示损益账户方案"),
    ],
    "prcp_coa_node": [  # 资负表账户册（部分）
        (1, "1100", "现金", None, 1, "ASSET", "/1/"),
        (1, "1101", "库存现金", 1, 2, "ASSET", "/1/1/"),
        (1, "1102", "央行存款", 1, 2, "ASSET", "/1/2/"),
        (1, "1200", "贷款", None, 1, "ASSET", "/2/"),
        (1, "1201", "公司贷款", 4, 2, "ASSET", "/2/1/"),
        (1, "1202", "个人贷款", 4, 2, "ASSET", "/2/2/"),
        (1, "2000", "负债", None, 1, "LIABILITY", "/3/"),
        (1, "2001", "客户存款", 7, 2, "LIABILITY", "/3/1/"),
        (1, "3000", "权益", None, 1, "EQUITY", "/4/"),
    ],
    "prcp_rpt_report": [
        ("RPT_BS", "资负表", "BALANCE", 1),
        ("RPT_PL", "损益表", "INCOME", 2),
        ("RPT_KPI", "关键指标报表", "INDICATOR", 1),
    ],
    "prcp_kpi_definition": [
        ("KPI_NIM", "净息差", 3, "(利息收入-利息支出)/生息资产", "PERCENT"),
        ("KPI_CAR", "资本充足率", 3, "资本/风险加权资产", "PERCENT"),
        ("KPI_ROA", "资产收益率", 3, "净利润/总资产", "PERCENT"),
        ("KPI_LCR", "流动性覆盖率", 3, "优质流动性资产/30天现金净流出", "PERCENT"),
    ],
}
```

---

## 12. 数据库迁移策略

### 12.1 v1 → v2 数据迁移

v1 已有 5 张表（prcp_group/position/rule/task/task_log）→ v2 全部废弃：

```sql
-- v2.0 init_db.py 中：
DROP TABLE IF EXISTS prcp_task_log;
DROP TABLE IF EXISTS prcp_task;
DROP TABLE IF EXISTS prcp_rule;
DROP TABLE IF EXISTS prcp_position;
DROP TABLE IF EXISTS prcp_group;

-- 然后创建 v2 全部 29 张表
-- 重建 admin 用户
```

> ⚠️ **注意**：v1 数据**全部丢失**，因为业务定位完全改变。需要用户确认。

### 12.2 增量升级（v2.x → v2.y+1）

后续小版本只 ALTER TABLE 加列，**不会** DROP。

---

## 13. 索引与性能

### 13.1 关键索引

| 表 | 索引 | 用途 |
|---|---|---|
| `prcp_coa_node` | `uk_scheme_code` | 方案内编码唯一 + 查询 |
| `prcp_coa_node` | `idx_path` | 树形查询（LIKE /path/%） |
| `prcp_data_balance` | `uk_node_date` | 节点 + 日期唯一 |
| `prcp_data_balance` | `idx_date` | 按日期聚合 |
| `prcp_kpi_value` | `uk_kpi_date_ver` | 指标 + 日期 + 版本唯一 |
| `prcp_rpt_item` | `idx_path` | 报表表项树形查询 |

### 13.2 大表预估

| 表 | 数据量（3 年） | 备注 |
|---|---|---|
| `prcp_data_balance` | 5000 行 | 500 节点 × 12 月 × 3 年 |
| `prcp_data_*` (6 张) | 30000 行/张 | 500 表项 × 12 月 × 3 年 |
| `prcp_kpi_value` | 6000 行 | 50 指标 × 12 月 × 3 年 × 版本 |
| `prcp_model_train_task` | < 100 行 | 训练任务较少 |

---

## 14. 总结

- **29 张表**覆盖 7 大业务模块 + 系统管理
- **3 张树形表**（COA / RPT / 子查询）用物化路径 +递归查询
- **JSON 字段**用于多值关联（COA 节点列表、加载指标列表、反算结果）
- **LONGTEXT**用于模型元数据和反算结果（结构灵活）
- **冗余字段**（item_count / node_count / progress）加速列表渲染
- **审计日志**所有写入都记录